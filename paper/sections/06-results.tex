\section{Results}
\label{sec:results}

\noindent\textit{This section is a skeleton. No model has yet been scored
on real data; every table is a placeholder hook for a fragment that
\texttt{scripts/analyze.py} will generate from the per-cell results files.
No number in this section is to be typed by hand.}

\subsection{Validation gates}

Gates 1--3 (Section~\ref{sec:design-gates}) passed before any real-data
result was read. The one substantive finding from gate~1---the mild
over-dispersion of two-stage SVD Lee--Carter on Poisson data---is carried
into the reading of the SVD-LC rows below.

\subsection{Calibration where nothing breaks: the stable regime}

Table~\ref{tab:h2-coverage} (stable panel) reports empirical coverage at
50/80/95\% and the interval score for every admissible cell across the
thirteen expanding origins 1990--2014. It establishes the reference level
for \Htwo\ and \Hfive: how far each family--mechanism pair is from nominal
when the future resembles the past.
\todo{stable-regime run not yet executed; requires runner defects 1--5 in
the defect ledger (STATUS.md) closed first}

\subsection{H1: ranking by RMSE versus ranking by CRPS}

\begin{table}[htbp]\centering
\caption{Shift regime: rank of each family by RMSE on log rates and by CRPS,
with the Spearman correlation and the number of top-3 inversions.}
\label{tab:h1-rankings}
\inputtable{tab-h1-rankings}
\end{table}

The table will show whether the family ordering that the literature's
default metric produces survives the move to a proper score.
\todo{result pending}

\subsection{H2: marginal coverage across the break}

\begin{table}[htbp]\centering
\caption{Empirical coverage of nominal 50/80/95\% intervals and mean interval
score, by family and mechanism, in the stable and shift regimes.}
\label{tab:h2-coverage}
\inputtable{tab-h2-coverage}
\end{table}

The table will show, for every admissible cell, the shortfall from nominal in
each regime and the shift-minus-stable difference with its wild-cluster
bootstrap interval. \todo{result pending}

\subsection{H3: joint path coverage}

\begin{table}[htbp]\centering
\caption{Marginal coverage at each horizon $h=1,\dots,5$ against joint path
coverage over the whole trajectory, nominal 95\%, shift regime.}
\label{tab:h3-joint}
\inputtable{tab-h3-joint}
\end{table}

The table will show the gap between the coverage a mortality paper reports
and the coverage a liability depends on, for every family, and whether the
copula conformal band closes it. \todo{result pending}

\subsection{H4: where in age the intervals fail}

\begin{table}[htbp]\centering
\caption{Shift-regime coverage of nominal 95\% intervals by age band
($0$--$24$, $25$--$64$, $65$--$99$), by family and mechanism.}
\label{tab:h4-age}
\inputtable{tab-h4-age}
\end{table}

The table will show whether the failure concentrates at the ages that carry
annuity liabilities. \todo{result pending; runner currently emits only
scalar mean coverage; per-age-band columns are defect 1 in the ledger}

\begin{figure}[htbp]\centering
\todoinline{figure \texttt{fig-calibration-by-age.pdf}: single-age coverage
curves, shift versus stable, one panel per family}
\caption{Calibration by single age, nominal 95\%, shift (solid) versus
stable (dashed), one panel per family; mechanisms as line styles.}
\label{fig:calibration-by-age}
\end{figure}

\subsection{PIT histograms and the Murphy decomposition}

\begin{table}[htbp]\centering
\caption{Randomised PIT decile frequencies and Kolmogorov--Smirnov distance
from uniformity, pooled within population, by family and mechanism and
regime.}
\label{tab:pit}
\inputtable{tab-pit}
\end{table}

\begin{table}[htbp]\centering
\caption{Murphy decomposition of the interval score into uncertainty,
resolution and miscalibration, shift regime.}
\label{tab:murphy}
\inputtable{tab-murphy}
\end{table}

The PIT table will show the \emph{shape} of each failure (U, hump or slope);
the Murphy table will show whether a poor score comes from miscalibration or
from lack of resolution. \todo{result pending; Murphy estimator is defect 2
in the ledger}

\subsection{Forecast comparison: Diebold--Mariano and the model confidence set}

\begin{table}[htbp]\centering
\caption{Pairwise Diebold--Mariano statistics on CRPS with wild-cluster
bootstrap $p$-values, and membership of the 90\% model confidence set, by
regime.}
\label{tab:dm-mcs}
\inputtable{tab-dm-mcs}
\end{table}

The table will show which differences between distributional cells survive
clustering by population, and how the set of not-outperformed models
changes between the stable and shift regimes. \todo{result pending;
per-horizon loss columns are defect 4 in the ledger}

\subsection{Twin crises: 1914--1922 beside 2020--2024}

\begin{table}[htbp]\centering
\caption{Placebo and shift regimes side by side: coverage of nominal 95\%
intervals at each horizon for the classical families, eleven and twenty
populations respectively; placebo reported pooled and by the pre-specified
strata (neutral register-based, belligerent total, civilian-only).}
\label{tab:twin-crises}
\inputtable{tab-twin-crises}
\end{table}

The table will show whether the pattern of interval failure at the earlier
break resembles the pattern at the later one---descriptively, without a
transfer regression---with the neutral stratum as the pandemic-only
analogue. \todo{result pending}

\subsection{Architecture or mechanism? Contrasts within sub-grids}

The payoff of the crossed design. Each contrast below is pre-registered and
computed as a paired difference within the admissible sub-grid named.

\begin{enumerate}
\item \emph{Conformal versus native, classical families only.} Does wrapping
  a classical fit in a conformal band change its shift-regime coverage, and
  at what cost in width?
\item \emph{Ensemble versus Monte Carlo dropout, neural families only.} Do
  two mechanisms on the same architecture give the same coverage?
\item \emph{Bootstrap versus native, classical families only.} Does adding
  parameter uncertainty move a classical interval toward nominal at the
  break?
\item \emph{Joint-path conformal versus split conformal, all families.} The
  H3 mechanism contrast.
\end{enumerate}
\todo{one table or figure per contrast; hooks to be added once the analysis
script fixes the layout}
